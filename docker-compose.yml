version: '3'

services:
  consul:
    image: hashicorp/consul:latest
    container_name: consul
    ports:
      - "8500:8500"  # Consul HTTP UI
      - "8600:8600"  # Consul DNS
      - "8600:8600/udp"
    networks:
      - consul-network

networks:
  consul-network:
    driver: bridge
